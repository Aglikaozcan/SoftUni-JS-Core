function htmlGrade (examPoints, homeworkCompleted, totalHomework) {

    let courseMaxPoints = 100;
    let examMaxPoints = 400;
    let totalPoints = 0;

    let grade = 3 + 2 * (totalPoints - maxPoints / 5) / (maxPoints / 2);

    if (examMaxPoints) {
        grade = 6;
    }
}

htmlGrade(300, 10, 10);